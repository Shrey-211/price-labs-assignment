{
  "devDependencies": {
    "@cypress/grep": "^3.1.3",
    "@shelex/cypress-allure-plugin": "^2.40.2",
    "@types/cypress": "^1.1.6",
    "@types/node": "^22.13.0",
    "@typescript-eslint/eslint-plugin": "^5.59.8",
    "cypress": "^14.0.1",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-real-events": "^1.14.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "cypress:run": "cypress run --browser chrome",
    "cypress:open": "cypress open --browser chrome",
    "cypress:run:headed": "cypress run --browser chrome --headed",
    "merge:reports": "mochawesome-merge cypress/reports/*.json -o cypress/reports/report.json",
    "generate:report": "marge cypress/reports/report.json -o cypress/reports",
    "full:run": "npm run cypress:run && npm run merge:reports && npm run generate:report",
    "allure:run": "cypress run --env allure=true",
    "allure:serve": "allure serve allure-results",
    "allure:generate": "allure generate allure-results --clean -o allure-report"
  }
}
